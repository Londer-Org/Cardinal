bin/
dist/
*.test
*.out
coverage.*

# Frontend
node_modules/
web/dist/

# Local config and anything that could hold a credential.
# Cardinal is an identity system; a leaked dev secret is a real incident.
.env
.env.*
*.pem
*.key
!**/testdata/**/*.pem
!**/testdata/**/*.key

.idea/
.vscode/
*.swp
.DS_Store

# Local server config; may hold a DSN. cardinal.example.toml is the template.
cardinal.toml

# Built UI is a build artifact, regenerated by `make ui`.
web/dist/assets/
web/dist/index.html
